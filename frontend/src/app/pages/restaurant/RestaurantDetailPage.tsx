import type { ReactElement } from 'react'

import { RestaurantDetail } from '../../../features/restaurants'

const RestaurantDetailPage = (): ReactElement => {
  return <RestaurantDetail />
}

export default RestaurantDetailPage
